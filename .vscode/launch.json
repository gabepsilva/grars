{
    "version": "0.2.0",
    "configurations": [
        // Debug insight-reader (F5 default) — Use for normal debugging.
        // Variables, stack traces, and stepping work as expected.
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug insight-reader",
            "cargo": {
                "args": ["build", "--bin=insight-reader", "--package=insight-reader"],
                "filter": {
                    "name": "insight-reader",
                    "kind": "bin"
                }
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        },
        // Debug insight-reader (release) — Use when debugging performance issues or bugs
        // that only appear in release builds. Note: the optimizer may inline
        // functions and eliminate variables, making debugging harder.
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug insight-reader (release)",
            "cargo": {
                "args": ["build", "--release", "--bin=insight-reader", "--package=insight-reader"],
                "filter": {
                    "name": "insight-reader",
                    "kind": "bin"
                }
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}

